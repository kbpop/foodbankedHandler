# FoodBanked flask handler

This is to handle the backend requests

## How to get started

1. Run ```docker build -t my-python-app .```
2. Run - ```docker run --env-file .env -p 8080:8080 my-python-app```
3. Your application should now be running properly

## Making SQL requests

Remember that the password, db name and the password need to be the same here as for the flask app